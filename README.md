# Axora (RAG)

## Python
```
pip install pipreqs
pipreqs ./ --force --savepath=./extractor/requirements.txt
pipreqs ./rag --force --savepath=./requirements.txt
pipreqs ./reranker_service --force --savepath=./reranker_service/requirements.txt

```
